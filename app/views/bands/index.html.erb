<% @bands.each do |band| %>
  <ul style="padding:5px;">
    <li> <a class="band" href="<%= band_url(band) %>"><%= band.name %></a> </li><br>
  </ul>
<% end  %>

<a class="button" href="<%= new_band_url %>">Make a New Band</a>
